<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="0" name="choice">🍗</button>
    <button type="submit" value="1" name="choice">🥩</button>
    <button type="submit" value="2" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <% if (locals.recipe) { %>
      <h2><%= recipe.name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>Protein name, protein preparation : <%= recipe.ingredients.protein.name %> <%= recipe.ingredients.protein.preparation %></li>
        <li>Salsa name: <%= recipe.ingredients.salsa.name %></li>
        <li>Every other topping:</li>
        <% for( let index = 0; index < recipe.ingredients.toppings.length; index++ ) { %>
          <%= recipe.ingredients.toppings[index].quantity %> <%= recipe.ingredients.toppings[index].name %>
          <br>
        <% } %>
      </ul>
    <% } else { %>
    <h2>Pick your favourite taco ingredient 👆</h2>
    <% } %>




  </div>
</body>

</html>